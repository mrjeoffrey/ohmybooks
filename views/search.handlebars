{{!-- {{#if title || author || genre || isbn}} --}}
{{!-- {{#each books as |book|}} --}}
  <section id="search" class="search-wrap">
      <h1>Find A Book</h1>
      <form action="/book/search" class="search-form">
        <i class="fas fa-search"></i>
        <input type="search" name="term" placeholder="Search by genre, title, author or isbn">{{}}
        <input type="submit" value="Search">
      </form>
  </section>

      <p>
        <strong>{{title}}</strong> by <strong>{{author}}</strong><br><br>
        {{description}}<br><br>
        Genre <strong>{{genre}}</strong> • ISBN <strong>{{isbn}}</strong>
       
      </p>
{{!-- {{/each}} --}}
{{!-- {{/if}} --}}