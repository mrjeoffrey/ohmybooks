const searchHandler = async () => {
    for (let i = 0; i < searchArray.length; i++) {
        const element = searchArray[i];

        
        
    }
}

document
    .querySelector('.searchBtn')
    .addEventListener('search', searchHandler);

